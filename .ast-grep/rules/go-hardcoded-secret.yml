id: go-hardcoded-secret
language: go
rule:
  pattern: |
    $VAR = "$LITERAL"
constraints:
  LITERAL:
    regex: (?i)(password=|token=|api_key=|secret=)
message: "Hardcoded secret detected. No hardcoded secrets -- use env vars and .env.example for samples."
severity: error
